#include <iostream>
using namespace std;
int main()
{
	int n;
	cin >> n;
	int a[20] = {0};
	int b;
	for (int i = 0; i < n; i++)
	{
		cin >> b;
		a[b]++;
	}
	for (int j = 0; j < n; j++)
	{
		if (a[j] == 2)
			cout << j;
	}
}
